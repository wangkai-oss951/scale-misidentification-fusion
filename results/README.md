Generated figures (`fig1_*.png` etc.) are written here when you run the reproduce scripts.

`paper_locked/` contains the manuscript CPU aggregates used for Fig. 1–3 and Tables 1–2. It does not contain audio or clinical prediction vectors.
